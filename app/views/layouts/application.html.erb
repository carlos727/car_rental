<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	<title><%= content_for?(:title) ? yield(:title) : "Car Rental" %></title>

	<%= stylesheet_link_tag    "application" %>
	<%= javascript_include_tag "vendor/modernizr" %>
	<%= csrf_meta_tags %>
</head>

<body>
	<header class="row">
		<nav class="top-bar" data-topbar>
			<ul class="title-area">
				<li class="name"><h1><a href="#">Car Rental</a></h1></li>
				<li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
			</ul>
		
			<section class="top-bar-section">
			<!-- Right Nav Section -->
				<ul class="right">
					<%if user_signed_in?%>
					<li><%= link_to current_user.fullname, edit_member_registration_path %></li>
					<li><%= link_to 'Log Out', destroy_user_session_path, method: :delete, class:'button' %></li>
				   <%else%>
					<li><%= link_to 'Sign Up', new_user_registration_path, class:'button' %></li>
					<li><%= link_to 'Sign In', new_user_session_path, class:'button' %></li>
					<%end%>
				</ul>
		
			<!-- Left Nav Section -->
			<ul class="left">
			  <li><%= link_to 'Cars', cars_path, class:'button' %></li>
			</ul>
		  </section>
		</nav>
	</header>
	
	<sectionm class="row">
		<div class="small-12 columns">
			<br>
			<% flash.each do |type, message| %>
				<div data-alert class="alert-box radius <%= flass_class type %>">
					<%= message %>
			  		<a href="#" class="close">&times;</a>
				</div>
			<% end %>

			<%= yield %>
		</div>
	</section>
	<%= javascript_include_tag "application" %>
  </body>
</html>